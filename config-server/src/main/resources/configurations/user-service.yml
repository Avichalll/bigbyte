spring:
  data:
    mongodb:
      uri: ${MONGODB_CONNECTION_STRING}
      database: bigbite
mongodb:
  ssl:
    enabled: true 

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true